<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lyl.play.mapper.TwitterNftMapper">
    <select id="selectByOrderNo" resultType="com.lyl.play.entity.TwitterNftRecord">
        select * from twitter_nft_record
        where creator_tag = 0 and order_no = #{orderNo}
    </select>

    <update id="upStatus" >
        update twitter_nft_record set status=#{status},update_time=now()
        <if test="status==3">
            ,mint_time=now()
        </if>
        where id=#{tokenId}
    </update>
</mapper>